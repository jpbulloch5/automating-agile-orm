---
- name:
  hosts: all
  tasks:

  - name: Ensure PostgreSQL is present
    become: yes
    apt:
        name: {{ items }}
        state: latest
    with_items:
     - postgresql
     - postgresql-contrib
